{
  "name": "polymarket-tests",
  "version": "1.0.0",
  "description": "Integration and backtest suites",
  "scripts": {
    "test:integration": "jest --testPathPattern=integration",
    "test:backtest": "jest --testPathPattern=backtest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
